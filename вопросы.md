## backend

Какие технологии использовать?
python + flask + (psycopg2 / asyncpg) + (ORM (SQLAlchemy) / raw SQL)

## frontend

Я хочу сделать это задание на React, но для будущего скорее всего придётся перейти на другую библиотеку, поэтому я сомневаюсь, стоит ли мне сейчас разбираться в react-библиотеке (проще, быстрее) или разбираться в стандартном js api от OpenLayers (универсальнее с точки зрения технологий). Как поступить?
